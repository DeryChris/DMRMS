# ── Python dependencies ──
# Install with: pip install -r requirements.txt
#
# ── System dependencies (NOT installed by pip) ──
# These tools are required for PDF → image conversion (AI document verification).
# Run the platform-specific setup script to install them:
#
#   Windows:  powershell -ExecutionPolicy Bypass -File setup.ps1
#   Linux:    chmod +x setup.sh && ./setup.sh
#   macOS:    chmod +x setup.sh && ./setup.sh
#
# Manual alternatives:
#   pdftoppm (poppler-utils) — single binary, recommended
#   ImageMagick + GhostScript — PHP extension, in-process conversion
#
# See docs/setup.md §1.1 for detailed installation steps.

fastapi>=0.115.0
uvicorn>=0.30.0
openai>=1.50.0
pillow>=12.0.0
python-multipart>=0.0.12
python-dotenv>=1.0.1
httpx>=0.27.0
pydantic>=2.9.0
pydantic_settings>=2.14.0
